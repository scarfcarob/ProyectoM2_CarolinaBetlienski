
//import process from 'node:process';
// Cargamos el entorno de manera aislada por si corremos este archivo solo: `node test-connection.js`


//try {
    //process.loadEnvFile();
//} catch (error) {
    // Silencioso o advertencia si no hay .env
//}

//import pool from './db.js';


//async function testConnection(){
    //try {
        //const result = await pool.query('SELECT NOW()');
        //console.log('Conexión exitosa a PostgreSQL');
        //console.log('Hora de servidor de base de datos:', result.rows[0].now);
        
        //await pool.end();
    //} catch (err) {
        //console.error('Error al conectar a PostgreSQL:', err.message);
    //}
//}

//testConnection();